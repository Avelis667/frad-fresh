#!/usr/bin/env sh
# ??????? ? ????
npx gitleaks detect --source . --redact || { echo "gitleaks failed"; exit 1; }

# ???? ????? (??? ??????????? ? CI-????)
npx prettier -c . || exit 1
npx eslint . || true   # ?? ????? ?????? ?? ?????? ?????
exit 0
